module.exports = function(grunt) {

    "use strict";

    // Load plugins
    require('load-grunt-tasks')(grunt);

    var appConfig = {

    };

    // Init Config
    grunt.initConfig(appConfig);

    // Register tasks
    grunt.registerTask();

};